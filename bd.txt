create DATABASE coffee_db;
use coffee_db;

CREATE TABLE users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_name VARCHAR (100),
  email VARCHAR(200) UNIQUE,
  user_password VARCHAR(50),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE coffees (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT,
  coffee_name VARCHAR(200) NOT NULL,
  roaster VARCHAR(200) NOT NULL,
  country VARCHAR(200) NOT NULL,
  region VARCHAR(200) NOT NULL,
  producer VARCHAR(200) NOT NULL,
  variety VARCHAR(200) NOT NULL,
  coffee_process  ENUM('washed','natural','honey','wet-hulled','anaerobic') DEFAULT ('washed'),
  roast_level ENUM('light', 'medium-light', 'medium', 'medium-dark', 'dark') DEFAULT ('medium'),
  roast_date DATE,
  roaster_notes VARCHAR(200) NOT NULL,
  status BOOL,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE SET NULL
);

CREATE TABLE comments (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  coffee_id INT NOT NULL,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (coffee_id) REFERENCES coffees(id) ON DELETE CASCADE
);

CREATE TABLE brew_logs (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT NOT NULL,
  coffee_id INT NOT NULL,
  brewed_at DATE,
  method ENUM('Pour-Over', 'Espresso', 'French-Press', 'AeroPress', 'ColdBrew', 'Other') DEFAULT ('Other'),
  dose_g TINYINT,
  water_g TINYINT,
  grind_setting VARCHAR(200) NOT NULL,
  water_temp_c TINYINT,
  brew_time_sec TINYINT,
  notes SET('roasted', 'spices', 'nutty/cocoa', 'sweet', 'floral', 'fruity', 'green/vegetative', 'sour/fermented', 'other') DEFAULT('other'),
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
  FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE,
  FOREIGN KEY (coffee_id) REFERENCES coffees(id) ON DELETE CASCADE
);


INSERT INTO users (user_name, email, user_password)
VALUES
  ('Carlos Mendes', 'carlos.mendes@email.com', 'coffee123'),
  ('Sofia Ribeiro', 'sofia.ribeiro@email.com', 'latte456');

INSERT INTO coffees (user_id, coffee_name, roaster, country, region, producer, variety, coffee_process, roast_level, roast_date, roaster_notes, status)
VALUES
  (1, 'Cerrado Sunrise',     'Torra Viva',       'Brazil',   'Cerrado Mineiro',  'Fazenda São João',    'Yellow Bourbon',  'natural',  'medium',       '2026-05-18', 'sweet, fruity, nutty',           true),
  (1, 'Yirgacheffe Bloom',   'Nordic Roast Co',  'Ethiopia', 'Yirgacheffe',      'Konga Cooperative',   'Heirloom',        'washed',   'light',        '2026-04-10', 'jasmine, bergamot, lemon zest',  true),
  (1, 'Huila Valley Dark',   'Café del Monte',   'Colombia', 'Huila',            'El Paraíso Farm',     'Castillo',        'honey',    'medium-dark',  '2026-03-22', 'chocolate, caramel, dried fruit', false),
  (1, 'Sumatra Wet-Hulled',  'Island Beans',     'Indonesia','Aceh',             'Gayo Highlands Co-op','Typica',          'wet-hulled','dark',        '2026-02-15', 'earthy, cedar, dark chocolate',  true),
  (1, 'Gesha Anaerobic',     'Altitude Works',   'Panama',   'Chiriquí',         'Hacienda La Esmeralda','Gesha',          'anaerobic','medium-light', '2026-05-30', 'tropical fruit, wine, rose',     true);

INSERT INTO brew_logs (user_id, coffee_id, brewed_at, method, dose_g, water_g, grind_setting, water_temp_c, brew_time_sec, notes)
VALUES
  (1, 1, '2026-06-15', 'Pour-Over',    18, 120, 'medium-fine',  93, 210, 'fruity,sweet'),
  (1, 2, '2026-06-16', 'AeroPress',    15, 100, 'fine',         88, 90,  'floral,sour/fermented'),
  (2, 3, '2026-06-17', 'Espresso',     18, 36,  'extra-fine',   92, 28,  'roasted,nutty/cocoa'),
  (2, 5, '2026-06-18', 'French-Press', 30, 120, 'coarse',       96, 240, 'sweet,fruity');
